# TASK-028 Checkpoint D-Lite 结构与响应式证据

- 日期：2026-08-17
- 结论：产品负责人有限通过；TASK-028 按页面迁移层面收口为 `Done`
- 证据基线：`main`，HEAD `bb2a125ea80ff85e6805a30830ac1cf4ac51dc76`
- 证据范围：页面编排、两条路径互斥、次级内容层级、实际视口与横向溢出

## 运行边界

本轮使用本地演示模式的进程级环境覆盖启动页面，未修改 `.env.local`；服务已停止。未上传真实客户资料，未触发真实 OCR/保存闭环，未切换双账号或第二租户，未排查浏览器控制层，也未启动新的 Agent。运行证据来自当前 HEAD 的浏览器 DOM/视口检查；截图由本轮 Codex 浏览器会话取得。

## 页面编排证据

| 场景 | 实际视口 | 观察结果 |
|---|---:|---|
| 默认入口 | 1440×900 | `scrollWidth=clientWidth=1440`；仅案件资料主入口和 Excel 批量台账次入口；无可见文件输入；最近记录/高级设置为次级内容 |
| 默认入口 | 768×900 | `scrollWidth=clientWidth=768`；路径卡片堆叠；无横向溢出；无可见文件输入 |
| 默认入口 | 390×844 | `scrollWidth=clientWidth=390`；路径卡片堆叠；无横向溢出；无可见文件输入 |
| `flow=case` | 390×844 | 仅案件资料上传路径；可见本人资料 PDF/图片与案件申请 Excel 两类资料控件；无批量台账入口；无横向溢出 |
| `flow=ledger` | 390×844 | 仅 Excel 批量台账上传路径；可见一个批量台账文件控件；无本人资料/案件申请 Excel 控件；无横向溢出 |
| 返回路径选择 | 390×844 | “更换路径”原生链接唯一且可用，返回 `/import-center`；最近记录与高级功能不与当前主任务竞争 |
| 处理中恢复视图 | 当前任务演示记录 | 显示当前任务、处理中状态和安全离开/恢复入口；未把 URL 参数单独当作完成事实 |

默认安静状态未发现百分比进度文本、`progressbar` 或伪整体 KPI。高级映射、历史、附件和运维内容保留在次级区域。

## 键盘证据边界

两条路径均为具备有效 `href` 的原生链接，链接可聚焦语义已核对，点击可进入对应路径。浏览器控制层无法让 Tab 在页面中移动焦点，因此没有合格的运行级 Tab/Enter 或返回焦点证据；本轮不重测，也不把静态语义写成完整无障碍通过。

> 原生链接及可聚焦语义已核对；真实 Tab、Enter 和浏览器返回焦点尚未取得合格运行证据，统一进入全产品迁移后的批次回归。本任务不宣称完整键盘可访问性通过。

## 未宣称通过的闭环

- 真实上传、OCR 准确率、保存写入、案件归属、批量导入和输出链路未在本轮验证。
- 422/503、权限/会话和任务不存在的完整恢复契约进入全产品迁移后的批次回归；不得把本轮页面状态证据解释为恢复闭环通过。
- 任务状态仍须区分已收到、处理中、需处理、等待确认、已写入和本次完成；不得由 `mapped`/`completed` 单独推导业务完成。
- 后续若真实物理键盘发现 Tab、Enter 或返回焦点缺陷，应建立独立缺陷修复，不重新否定本任务的页面结构迁移结果。

## 收口与范围

- 未修改 actions、API、处理器、数据库、认证、权限、租户或其他页面。
- `src/app/clients/page 2.tsx` 保持未跟踪、原状，并显式排除本次提交。
- TASK-020 和输出产品专题继续冻结；不自动启动下一页面任务。
