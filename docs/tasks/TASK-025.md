# TASK-025 / W1：信息整理中心 List Report 参考实现

- 状态: In Progress
- 优先级: P0
- 负责人: 技术项目经理 / 实现 Agent / 独立审查 Agent
- 依赖关系: TASK-024 已 Done；TASK-020 继续独立 Blocked，不是本任务的修复范围
- 当前允许阶段: Checkpoint B 已批准；Checkpoint C 进入待归属权威修正，先完成文档写回，再进行最小代码修正和最终验收
- Checkpoint A 状态: 已通过（2026-08-16）；审计结论已纳入 W1 目标结构草案
- 参考矩阵: [`UI_GOV_002B_LAYOUT_FLOORPLAN_MATRIX_V2_2026-08-16.md`](../operations/UI_GOV_002B_LAYOUT_FLOORPLAN_MATRIX_V2_2026-08-16.md)
- Checkpoint B 目标规格: [`TASK-025_W1_TARGET_STRUCTURE_2026-08-16.md`](../operations/TASK-025_W1_TARGET_STRUCTURE_2026-08-16.md)
- Checkpoint B 状态: 已批准（2026-08-16）；批准目标结构与交互规格，不代表页面已实现

## 任务名称

W1 信息整理中心 List Report 参考实现

## 背景和用户结果

当前矩阵已将 `/organize-center` 确认为 List Report 主页面，但页面仍包含异常处理和多个对象类型入口。用户需要先找到案件、主体或物件，再进入后续处理，而不是在信息整理中心直接编辑全部对象。

用户主要任务是：

> 找到案件、主体或物件，并进入后续处理。

信息整理中心负责查找、筛选、结果浏览和进入对象；不负责承担案件详情编辑，也不合并案件、主体和物件的业务权威。

## 任务目标

将 `/organize-center` 作为 Broker Desk 的第一个 List Report 参考实现候选，在不改变业务数据和权限语义的前提下验证统一的筛选、结果、空态和错误结构。

## 本次范围

- `/organize-center` 的主 Floorplan 为 `List Report`。
- 待处理、异常或需要补全的可靠对象可以在页面局部使用 `Worklist` 结构，但不能把整个页面重新定义为 Worklist。
- W1 只交付案件、主体、物件三个可靠对象入口；待归属资料入口、列表、数量和状态徽章因缺少具体对象归属权威而冻结，不代表资料为零或页面已完成。
- `/import-center` 继续负责导入任务、处理状态、映射和失败恢复；W1 不把 `targetEntity`、案件导入关联或处理状态解释为具体主体/物件归属。
- 案件、主体、物件继续使用各自的数据来源、权限和领域规则。
- `OrganizeCenterObjectBrowser` 保持领域组件，不直接改造成全站通用对象浏览器。
- 只建立通用的 Filter Bar、结果结构、空态和错误状态组合；不建立第二套业务数据或保存逻辑。
- 进入对象后，浏览器历史返回必须保留 `type`、`q`、`lifecycle`、`page`、列表滚动和触发对象链接焦点；不建立或恢复独立选择状态。一级导航“整理信息”仍返回对象选择器并重置临时列表上下文。

## 明确不做什么

- 不修改案件保存、案件字段、案件确认或 `confirmedDataJson`。
- 不触碰 TASK-020，不实施输出中心、申请书预览、下载门禁或案件级确认。
- 不修改权限模型、租户隔离、数据库结构、API 契约或 149 项字段体系。
- 不迁移其他 List Report 页面，包括客户、主体、物件、报价、模板库和审计日志。
- 不把所有结果做成卡片；表格、结果行和窄屏等价结构按实际任务决定。
- 不进行全站 CSS 替换、导航重写、全局换皮或组件机械替换。
- 不因已有页面结构而默认现有交互正确；必须先取得真实流程证据。

## 依赖关系

- 依赖 TASK-024 已完成的 Layout System Approved baseline。
- 不依赖 TASK-020 的输出闭环；本任务不得修改或解除 TASK-020。
- 不依赖 TASK-019 的外部隧道；Checkpoint D 的真实权限和租户证据必须使用合法测试环境，缺失时标记为人工验证。

## 验收标准

### Checkpoint A：当前流程审计

在真实已登录环境中，只读观察当前正式 `/organize-center`，不得修改代码、目标图或页面。

必须连续记录以下流程：

1. 进入信息整理中心；
2. 记录初始状态；
3. 输入筛选条件；
4. 记录有结果状态；
5. 记录无结果状态；
6. 记录异常或错误状态；
7. 打开一个案件、主体或物件；
8. 返回信息整理中心，分别核对浏览器历史返回和一级导航返回的筛选、滚动、分页和触发链接焦点语义。

交付证据：

- 每一步至少一张桌面截图；
- 关键路径补充窄屏和键盘证据；
- 记录 URL、时间、视口、登录/租户测试边界和当前 HEAD；
- 区分代码事实、截图观察、推断和未验证假设；
- 若没有结果、异常或权限状态，明确记录为证据缺口，不伪造通过。

Checkpoint A 未完成前：

- 不制作目标图；
- 不修改 `src/`；
- 不启动实现 Agent 或独立审查 Agent；
- 不把当前矩阵结构报告当作页面体验验收。

### Checkpoint B：目标结构与产品批准

当前状态：Checkpoint B 已批准；当前只进行 Checkpoint C 前置准备，正式代码实现须在两份遗留治理文档差异完成独立归属、工作区形成明确干净基线后另行开始。

仅在 A 证据完成后提出一套目标结构，至少说明：

- Filter Bar 的字段、清除和返回行为；
- 按案件、主体、物件分别定义权威来源和禁止推导的含义；待归属资料不纳入 W1 状态契约，未来恢复需独立产品和数据决策；
- 结果计数、排序和加载方式；分页使用 URL 的 `page` 参数承载；
- 案件、主体、物件如何保持不同业务语义；
- 空态、无结果、权限拒绝和系统错误的区别；
- 打开对象后的返回路径和上下文恢复；
- 桌面、平板、手机和中日韩长文本的结构。

目标结构必须获得产品负责人批准后，才能进入 C。

### Checkpoint C 前置准备与实施边界

Checkpoint B 已批准后，才允许准备单页实现；当前仍未进入代码实现。实现前必须先形成明确干净基线。形成后，最多启动一个实现 Agent，只实现 `/organize-center`，完成一次明确交付后退出并报告活跃 Agent 回到 0。

允许优先检查或修改：

- `src/app/organize-center/page.tsx`；
- `src/components/organize-center-object-browser.tsx`；
- 已批准的 `ui-foundation` 组件，仅在确有必要且不改变其他页面时复用。

实施必须保留：对象选择器、案件/主体/物件三个独立 List Report、对象名称原生链接、Tab/Enter 原生行为、无独立 selected 状态、URL 的 `type/q/lifecycle/page`、浏览器返回的查询/分页/滚动/触发链接焦点恢复、一级导航返回模块首页、次级归档和权威状态表达。

本轮最小修正还必须删除待归属列表构建、案件关联差集判断以及其计数/排序影响。访问 `/organize-center?type=inbox` 时不得返回推导列表；可以诚实提示当前不提供可靠归属列表并引导 `/import-center`，或安全回到对象选择器。不得修改 `/import-center`。

状态契约只定义用户可见语义，不授权新建业务完成算法。只能使用现有服务端或领域权威可以证明的状态；不得用 `reviewed` 等局部字段推导案件完成，不因未发现错误自动显示“当前无整理问题”，不建立跨案件/主体/物件的统一状态计算器，不覆盖既有领域优先级，也不显示或推导输出资格。推荐状态缺少可靠数据支持时必须停止并报告。

禁止修改全局导航、案件详情、保存动作、API 契约、权限、Clerk、租户隔离、数据库、149 项字段体系、TASK-020、输出/预览/下载/打印、其他 List Report、全局 CSS 或第二套 Token；禁止增加高级筛选、批量操作、侧栏预览、第二套对象模型或额外页面。

### Checkpoint C：单页实现

只允许实现 `/organize-center`：

- 接入已批准的 Layout System Token 和基础组件；
- 复用或提取通用 Filter Bar、结果结构、空态和错误状态；
- 保留 `OrganizeCenterObjectBrowser` 的领域行为和数据权威；
- 保留案件、主体、物件的独立入口和业务语义；
- 保留进入对象后的返回上下文。

不得借机修改其他页面、公共业务保存逻辑、案件页面、输出页面、权限或租户逻辑。

### Checkpoint D：最终验收与独立审查

必须验证：

- 桌面、平板、手机布局；
- 中文、日文、韩文长文本；
- Tab、Enter、Escape 和焦点恢复；
- 有结果、无结果、空数据、错误和加载状态；
- 打开案件、主体、物件后的返回路径；
- 返回后 `type`、`q`、`lifecycle`、`page`、列表滚动和触发对象链接焦点；不恢复独立选择状态；
- 正常权限拒绝；
- 第二租户不能读取或操作其他租户对象；
- 现有业务数据、案件保存和输出行为没有回归；
- lint、typecheck、build、workflow rules、必要业务回归和 `git diff --check`。

实现 Agent 完成并退出后，才可启动一个独立审查 Agent。独立审查只检查 TASK-025，不修改代码、不扩展到其他页面、不审查或关闭 TASK-020。审查完成后立即退出。

## 必须保留的权威

| 领域 | 权威 |
|---|---|
| 案件 | 当前案件数据、案件权限和案件页面既有保存语义 |
| 主体 | 主体记录、角色、归档状态和租户边界 |
| 物件 | 物件记录、案件/主体关系和租户边界 |
| 页面筛选 | URL 或当前页面已有筛选语义；不得另建持久化筛选数据库 |
| 异常状态 | 领域数据和服务端返回；视觉组件只负责表达 |

## 停止条件

出现以下任一情况立即停止并报告：

- 需要修改案件保存、输出、权限、租户或数据库；
- 需要合并案件、主体、物件的数据模型；
- 需要把 `OrganizeCenterObjectBrowser` 改造成跨领域万能组件；
- 无法在真实登录环境中确认返回上下文；
- 需要通过关闭认证、租户隔离或错误处理来完成页面；
- 需要同时迁移其他 List Report 或建立全局 CSS 替换。

## 预计涉及的模块

- `src/app/organize-center/page.tsx`：只在 Checkpoint C 获批后允许修改。
- `src/components/organize-center-object-browser.tsx`：保持领域行为；只有明确的单页结构适配才可修改。
- `src/components/ui-foundation/*` 和 Layout System：只复用已批准基础，不建立第二套 Token。
- `src/components/app-nav.tsx`、案件页面、输出页面、数据库和 API：不在本任务修改范围内。
- `docs/tasks/TASK-025.md`、`BACKLOG.md`、`docs/operations/CURRENT_WORKING_CONTEXT.md`：用于任务和交接记录。

## 风险和注意事项

- 静态结构不能证明筛选、滚动、返回、权限或租户行为；Checkpoint A 必须取得真实页面证据。
- 案件、主体、物件都能从信息整理中心进入，但这不证明它们可以共用一个数据权威或编辑器。
- 局部异常 Worklist 不能反向夺取 List Report 的主任务；正常结果应保持安静，异常才突出。
- 当前输出中心仍有 TASK-020 门禁；不得因对象入口存在而顺手修复输出链路。
- 任何 `src/`、数据库、权限、租户、输出或其他页面差异都表示越界，必须停止。

## 回退与交接

- A、B 阶段无生产代码变更，回退方式为不进入下一检查点。
- C 阶段必须使用独立提交，明确记录允许修改文件和回退点。
- D 阶段归档截图、运行日志和浏览器断言；无法验证的外部权限或租户行为必须标记为人工验证。
- 任务完成后更新 BACKLOG 和 CURRENT_WORKING_CONTEXT；未通过则保持 `In Review` 或记录具体阻塞，不得标记 Done。

## 验证命令

- `npm run test:workflow-rules`
- `git diff --check`
- `git status --short --branch --untracked-files=all`
- Checkpoint A：真实登录页面截图、URL/时间/视口/HEAD记录和关键路径证据。
- Checkpoint D：桌面、平板、手机、键盘、权限、租户和业务回归证据。

## 当前状态

Checkpoint A 已通过；Checkpoint B 已批准。产品负责人已裁决 W1 只交付案件、主体、物件三个对象类型；待归属资料因缺少具体对象归属权威而冻结，不代表数据为零或页面已完成。`e7aba60` 仅为前一轮状态修正提交，不能作为最终实现提交；本轮先完成文档写回，再进行待归属边界的最小代码修正和最终验收。TASK-020 和输出专题不受影响。
