# TASK-036：全产品 Layout System 统一批次回归

- 状态: Blocked
- 优先级: P0
- 负责人: 技术项目经理
- 当前阶段: 统一运行回归与独立审查
- 前置任务: TASK-024、TASK-025、TASK-027–TASK-035
- 基线: `f671cff6a50fb6e38f440c61c62f10b136cc69f8`
- 范围: 已迁移的 22 个正式页面族、Floorplan 代表页及统一运行证据
- 排除: 输出/报价/模板专题、TASK-020、认证/权限/租户/数据库重构、`src/app/clients/page 2.tsx`

## 任务名称

全产品 Layout System 统一批次回归

## 背景和用户结果

本任务只验证已完成结构迁移的页面是否在真实浏览器中保持正确 Floorplan、响应式层级、状态语言、键盘焦点和安全返回，不重新设计页面、不扩大业务模型、不重复讨论矩阵归属。

## 本次范围

- 22 个已迁移页面逐路由冒烟：打开或出现符合身份条件的诚实 System State、标题和主要任务正确、无白屏/构建错误/已删除伪事实。
- Floorplan 代表页的 1440×900、768×900、390×844 结构、横向溢出和主要操作检查。
- 代表性 Tab/Enter/Escape、错误摘要焦点、保存/取消/浏览器返回焦点和筛选返回上下文。
- 一个共享 Responsive Form 的真实 macOS Kotoeri IME 检查。
- 安全非生产数据下的有限搜索、筛选、分页、保存/取消、真实状态和入口闭环。
- 证据报告、矩阵状态回写和统一回归清单更新。

## 明确不做什么

- 不启动新设计，不改变 Floorplan，不进行第二轮视觉优化。
- 不修改认证、权限、租户、数据库、migration、限流、API 或共享业务模型。
- 不创建第二租户、不反复切换账号、不使用伪造身份、不关闭认证。
- 不触碰 TASK-020、输出/报价/模板专题或 `src/app/clients/page 2.tsx`。
- 不为 P2 视觉问题扩展实现；涉及业务模型、权限、认证、数据库或输出专题的缺陷只记录为阻塞。

## 依赖关系

- Layout 规范：`docs/product/BROKER_DESK_LAYOUT_SYSTEM_V1.md`
- 页面归属：`docs/operations/UI_GOV_002B_LAYOUT_FLOORPLAN_MATRIX_V2_2026-08-16.md`
- 页面迁移基线：TASK-024、TASK-025、TASK-027–TASK-035
- 运行证据汇总：`docs/operations/UNIFIED_LAYOUT_BATCH_REGRESSION_CHECKLIST_2026-08-18.md`

## 环境启动策略

只允许一次有限恢复链：正常开发服务；同一命令一次合法提升权限运行；再失败时一次现有正式构建本地启动。仍失败即记录环境阻塞并停止环境排查。

## 恢复边界

- 已取得的 57 张截图、22 路由冒烟、三档宽度测量和键盘返回证据永久有效；恢复后不得全量重跑。
- 恢复只需要一个合法 Clerk Development 身份，且该身份对一个非生产工作区具有 `active membership`；不需要第二账号或第二租户。
- 第二租户隔离、跨租户拒绝和邀请激活属于平台级 QA，不再作为 Layout System 回归前提。
- 环境恢复后只补：一个 List Report 的搜索/筛选/分页/浏览器返回；一个 Responsive Form 的非法值/错误摘要焦点/合法保存/取消；一次真实 Kotoeri 组合输入；一个 Object Page 真实内容；一个 Worklist 真实状态；Board 的真实 `Client.stage`；Relationship Explorer 的显式关系；首页真实入口；Settings 既有设置保存。
- 不重复 22 路由全量截图；仅对补测发现的 P0/P1 回归受影响页面重跑。

## 当前状态口径

- Layout System 结构迁移：`Done`
- 响应式路由冒烟：`Pass`
- 真实业务运行验收：`Blocked`
- 输出专题：未开始

## 验收标准

- 22 个迁移路由完成冒烟，或对环境阻塞作逐项诚实记录。
- 每种 Floorplan 的代表页完成 1440/768/390 检查并记录视口、横向溢出和主要操作。
- 代表性键盘、焦点和 IME 证据完成；未取得的项目明确标记 `UNVERIFIED`。
- 无未解决的 Layout System 范围 P0/P1；P2 只登记。
- 独立只读审查区分真实运行证据、静态守卫和未验证项并通过。
- 报告和矩阵状态已写回；工作区只剩原有未跟踪 `src/app/clients/page 2.tsx`。

## 缺陷分级

- P0：数据损坏、安全、跨租户泄漏或无法启动。
- P1：主要流程不可用、移动端阻断、严重键盘/焦点问题或 Floorplan 回退。
- P2：轻微视觉、文案或非阻塞确认体验；只登记不优化。

## 允许的有限修复

仅允许修复已迁移页面范围内的、可证明属于布局或交互回归的 P0/P1；修复后只重跑受影响场景和全路由冒烟。任何需要业务模型、权限、认证、数据库、API、输出专题或新增保存逻辑的修复立即冻结并报告。

## 风险和注意事项

- 开发服务、Clerk、权限和租户环境可能阻断真实运行证据；不得通过关闭认证或伪造身份绕过。
- 测试数据必须为合法非生产记录；涉及危险操作时条件不足即记为 `UNVERIFIED`。
- 原始录屏不得进入仓库；只归档脱敏证据和私有文件哈希。

## 验证命令

- `npm run test:workflow-rules`
- `git diff --check`
- 已有相关专项契约脚本
- 统一服务启动链和浏览器回归记录于 `TASK-036_LAYOUT_REGRESSION_2026-08-18.md`

## 预计涉及的模块

- 已迁移页面及其直接页面组件（仅在批准的 P0/P1 回归修复时）
- 本任务回归报告和矩阵/上下文治理文档
- 必要的现有契约脚本；不默认新增业务逻辑

## Agent 与停止条件

- 先使用一个执行/验证 Agent；其退出后再使用一个独立只读审查 Agent；不得创建下级 Agent。
- 不启动第二轮视觉优化或输出专题。
- 若环境链路全部失败，任务保持 `Blocked`，不能以静态检查替代运行通过。
- 仅当运行证据、独立审查、报告和矩阵写回全部完成后才可标记 `Done`。

## 当前状态

已确认 `main` 与基线 HEAD 一致，工作区仅有原有未跟踪 `src/app/clients/page 2.tsx`，`.env.local` 被 `.gitignore` 忽略。统一浏览器回归已执行一次有限环境链；合法 Development 身份与请求租户不匹配，业务页面闭环无法取得，任务保持 `Blocked`，不能以静态检查替代运行通过。恢复时只补恢复边界列出的差额，不重跑既有截图与冒烟证据。
